export enum PermissionSubject {
	vod = "vod",
	dns = "dns",
	ansible = "ansible",
	workspace = "workspace",
}

export enum PermissionAction {
	CREATE = "create",
	READ = "read",
	UPDATE = "update",
	DELETE = "delete",
}
